ALTER TABLE HR.OPP_T_SETTING_USER DROP PRIMARY KEY CASCADE;

DROP TABLE HR.OPP_T_SETTING_USER CASCADE CONSTRAINTS;

CREATE TABLE HR.OPP_T_SETTING_USER (

USER_ID                  VARCHAR2(30 BYTE)  NOT NULL,
SETTING_TYPE_ID             VARCHAR2(100 BYTE)  NOT NULL,
VALUE                  VARCHAR2(1000 BYTE)  NOT NULL
);



CREATE UNIQUE INDEX HR.OPP_PK_SETTING_USER ON HR.OPP_T_SETTING_USER
(USER_ID,SETTING_TYPE_ID);

ALTER TABLE HR.OPP_T_SETTING_USER ADD (
CONSTRAINT OPP_PK_SETTING_USER
PRIMARY KEY
  (USER_ID,SETTING_TYPE_ID)
  ENABLE VALIDATE
);